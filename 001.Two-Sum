def twoSum( nums, target):
    """
    :type nums: List[int]
    :type target: int
    :rtype: List[int]
    """
    bequeath = {}
    for i, num in enumerate(nums):
        if target - num in d:
            return [bequeath[target - num], i]
        bequeath[num] = i
  
  
  #The second
  
  
